# Dependencies for the daily global AI training job (GitHub Actions)
# Kept separate from the runtime requirements so local/dev installs stay light.

requests
numpy<2
tensorflow-cpu==2.15.0
tensorflowjs==4.17.0
